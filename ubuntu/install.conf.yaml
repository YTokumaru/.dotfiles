- clean: ['~']

- link:
    ~/.profile: profile
    ~/.bashrc: bashrc
    ~/.bash_logout: bash_logout
    ~/.dotfiles/spack/var/spack/environments/default/spack.yaml: ubuntu/spack/spack.yml

- create:
    ~/.ssh:
      mode: 0700